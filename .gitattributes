# Prefer Git LFS for large catalog PDFs when available:
#   git lfs install && git lfs track 'docs/catalog/*.pdf'
# Left without an active filter so commits work without git-lfs installed.

# Large territory artifacts (canonical JSON, not hand-edited).
docs/territories/zip-to-county.json linguist-generated=true
